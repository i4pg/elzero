let flt = 10.4;

console.log(Math.floor(flt)); // 10
console.log(Math.trunc(flt)); // 10
console.log(parseInt(flt)); // 10
console.log(flt.toFixed()); // 10
console.log(
  Number.isNaN(flt).toString().length +
  Number.isNaN(flt).toString().length
); // 10, Same way with 5th assignment